import { FormConfig } from '../types/elementForm';
import { ObjectOfStringKey } from '../types/commonType';
/**
 * AIM: 获取 formItems 中的字段和默认值
 * @param  config - 表单配置对象
 * @param  defaultFormData - 默认值对象
 * @returns 由 formItems 的 prop 属性构成的对象
 */
declare const _default: (config: FormConfig, defaultFormData?: ObjectOfStringKey) => ObjectOfStringKey<any>;
export default _default;
