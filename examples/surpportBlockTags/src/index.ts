import { func3 } from "./utils";

/**
 * The summary section should be brief.
 *
 * @remarks
 *
 * The main documentation for an API item is separated into a brief
 * "summary" section optionally followed by an `@remarks` block containing
 * additional details.
 *
 * @param x The first parameter.
 * @param y The second parameter.
 *
 * @deprecated Use {@link func3} instead.
 *
 * @example
 * Here's an example with negative numbers:
 * ```ts
 * // Prints "0":
 * console.log("0");
 * ```
 *
 * @see https://tsdoc.org/pages/tags/see/
 *
 * @throws {@link Error}
 *
 * This exception is thrown if the input is not a valid ISBN number.
 *
 * @returns The arithmetic mean of `x` and `y`
 */
export function func<T, V>(x: string, y: string) {}
